type Query {
  books: [Book!]
  book(title: String!): Book
}

type Mutation {
  createBook(title: String!, author: String!): Book
}

type Book {
  title: String!
  author: String!
  link: String
  year: Int
}
